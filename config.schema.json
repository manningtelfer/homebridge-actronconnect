{
  "pluginAlias": "Homebridge Actronair",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "required": ["email", "password"],
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "Air Conditioning"
      },
      "email": {
        "title": "Email",
        "type": "string",
        "description": "ActronAir account email address",
        "format": "email"
      },
      "password": {
        "title": "Password",
        "type": "string",
        "description": "ActronAir account password"
      }
    }
  }
}
